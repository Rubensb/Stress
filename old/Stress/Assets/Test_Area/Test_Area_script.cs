using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test_Area_script : MonoBehaviour {

//setting GameplayManager
	private GameplayManager gameplayManager;
/*
//temp array
	private int[] TempA;
	private int[] TempB;
	private int[] TempAnew;
	private int[] TempBnew;
	// Use this for initialization
	void Start () {
		gameplayManager = GameObject.FindObjectOfType<GameplayManager>();
		TempA = new int[4] { 4, 4, 4, 4 };
		TempB = new int[4] { 3, 3, 3, 3 };
		TempAnew = new int[4] { 2, 8, 4, 9 };
		TempBnew = new int[4] { 3, 3, 3, 3 };
		Debug.Log(TempAnew[3]);
		TempAnew = gameplayManager.a_hand;
		Debug.Log(TempAnew[3]);

		for (int i = 0; i < 4; i++)
		{
			if (TempB[i] != TempBnew[i]){
				Debug.Log("----------WORKED----------");
			}
		}
	}

	// Update is called once per frame
	void Update () {
		//TempAnew = new int[] TempA.Clone();

	}*/
}
